targets:
  aggregates:
    kind: relation
    mapping: aggregates
    relation: aggregates

  aggregates_jdbc:
    kind: relation
    mapping: aggregates
    relation: aggregates_jdbc

  aggregates_delta:
    kind: relation
    mapping: aggregates
    relation: aggregates_delta
    mode: update
