#!/usr/bin/env bash

basedir=$(readlink -f "$(dirname "$0")"/..)
source "$basedir"/libexec/flowman-common.sh

APP_NAME="flowman-client"
APP_VERSION="${project.version}"

APP_JAR="$APP_NAME-$APP_VERSION-jar-with-dependencies.jar"

java -jar $APP_JAR "$@"
