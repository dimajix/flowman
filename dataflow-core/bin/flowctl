#!/usr/bin/env bash

basedir=$(readlink -f $(dirname $0/..))
source $basedir/libexec/df-common.sh

APP_NAME="dataflow"
APP_VERSION="1.0"
APP_MAIN="com.dimajix.dataflow.tools.control.Driver"

APP_JAR=$basedir/lib/"$APP_NAME-$APP_VERSION.jar"

spark_submit $APP_JAR $APP_MAIN "$@"
