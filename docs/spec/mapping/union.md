# Union Mapping

## Example

```
mappings:
  facts_all:
    kind: union
    inputs:
      - facts_rtb
      - facts_direct
```


## Fields
 * `inputs` **(mandatory)** *(list)*:
 * `columns` **(optional)** *(map:string)* *(default=empty)*:


## Description
